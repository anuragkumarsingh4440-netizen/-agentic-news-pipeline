"""Factories: read Settings, build the right concrete objects, inject them.

This is where the 'D' in SOLID becomes real — composition lives here so the
rest of the code only ever sees interfaces. Add a new provider => edit one
factory, nothing else.
"""
